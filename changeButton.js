document.getElementById("showButton").addEventListener("click", function () {
    if (showButton.innerHTML == "Hide Form Add New Student") {
        showButton.innerHTML = "Show Form Add New Student";
    } else {
        showButton.innerHTML = "Hide Form Add New Student";
    }
});